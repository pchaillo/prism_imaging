classdef SensorBase
   
    properties
        % Contain all the common properties of all depth sensor used for 3d data acquisition.
        %Nb_shoot_per_burst = 5
    end

    methods
        function sensor_co = connect(sensor) 
           % insert laser connexion and return connection object variable
        end
   end
end